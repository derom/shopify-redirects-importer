{
  "name": "shopify-redirects-importer",
  "version": "1.0.0",
  "description": "A script to import redirects into Shopify shop.",
  "main": "index.js",
  "author": "Denis Soldatov",
  "license": "MIT",
  "dependencies": {
    "@shopify/shopify-api": "^1.1.0"
  },
  "devDependencies": {
    "@types/node": "^14.14.33",
    "dotenv": "^8.2.0",
    "typescript": "^4.2.3"
  },
  "scripts": {
    "build": "npx tsc",
    "prestart": "yarn run build",
    "start": "node dist/index.js"
  }
}
